/**
 * Default activities enabled when the user skips the activity picker.
 * IDs must match entries in `@data/activities`.
 */
export const DEFAULT_ACTIVITY_IDS = ["fajr", "dhuhur", "asr", "maghrib", "isha"];
